@model FeatureViewModel
<h3 class="alert alert-warning">Edit @Model.EnglishName Feature</h3>
<form asp-action="Edit" id="form" data-id="@(Model?.Id ?? 0)" enctype="multipart/form-data">
    <partial name="_CreateEditDeleteFeature" />
    <br />
    <div>
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>